module Api
  module V1
    module Coaching
      class SessionsController < ApplicationController
        include ExceptionHandler
        include ActAsApiRequest
        include DeviseTokenAuth::Concerns::SetUserByToken

        before_action :authenticate_user!

        private

        def create_session
          # Get coach and coachee
          # Get time to start and end time
          # Validate if there is a conflict on the agenda of both participants
          # create the session or return error
        end
      end
    end
  end
end
